---
story_id: "2.3"
epic_id: "epic-2"
title: "Módulo Imobiliária (CRUD completo)"
status: InReview
created_by: "@sm (River)"
created_at: "2026-05-30"
ux_gate: required
---

# Story 2.3: Módulo Imobiliária (CRUD completo)

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [pattern_validation, code_review, accessibility_check, vitest_unit, e2e_playwright]
specialized_agents:
  - "@ux-design-expert"  # FASE OBRIGATÓRIA antes de @dev (ver UX Gate abaixo)
```

## 🚫 UX Gate — OBRIGATÓRIO

Esta story contém telas, tabela, formulário e páginas de CRUD.
**@dev BLOQUEADO** até que o artefato abaixo exista e esteja completo:

```
docs/stories/2.3-ux-spec.md
```

Delegue para `@ux-design-expert *design-story 2.3` antes de iniciar implementação.

## História

**Como** usuário autenticado (vendedor ou gerente),
**Quero** cadastrar, listar, visualizar, editar e remover imobiliárias,
**Para que** possa manter o cadastro de agências parceiras e vinculá-las a propostas de venda futuramente.

## Critérios de Aceitação

1. **Lista paginada:** página `/imobiliarias` exibe tabela com colunas: razão social, nome fantasia, CNPJ (mascarado), CRECI-J, e-mail e coluna de ações.
2. **Busca server-side:** campo de busca filtra por `legal_name`, `trade_name`, `cnpj` ou `creci_j` via query param; debounce de 300ms; reset para página 1 ao alterar busca.
3. **Paginação server-side:** mínimo 10 itens por página; exibe info "X–Y de Z imobiliárias"; botões de página funcionais.
4. **Skeleton rows** exibidos durante carregamento inicial e ao mudar filtros/página.
5. **Empty state** com mensagem em português exibido quando a lista está vazia (com e sem filtro ativo).
6. **Criação:** formulário em `/imobiliarias/novo` cria imobiliária via `POST /api/v1/agencies`; toast de sucesso e redirect para `/imobiliarias` ao concluir.
7. **Detalhe:** página `/imobiliarias/:id` exibe todos os campos da imobiliária (CNPJ mascarado, telefone formatado, nome fantasia se presente).
8. **Edição:** formulário em `/imobiliarias/:id/editar` atualiza via `PATCH /api/v1/agencies/{id}`; toast de sucesso e redirect para `/imobiliarias/:id` ao concluir.
9. **Soft-delete com confirmação:** botão de excluir abre `AlertDialog` em português antes de chamar `DELETE /api/v1/agencies/{id}`; toast de sucesso e redirect para `/imobiliarias` ao concluir.
10. **Validação Zod (agency.schema.ts):**
    - `cnpj`: CNPJ válido (usar `validateCNPJ` do api-client), campo obrigatório.
    - `legal_name`: 3–160 caracteres, obrigatório.
    - `trade_name`: 0–120 caracteres, opcional.
    - `creci_j`: 5–20 caracteres, obrigatório.
    - `email`: e-mail válido, opcional.
    - `phone`: formato E.164 (stripped de máscara), opcional.
11. **Máscara CNPJ:** reutilizar input/máscara já existente no módulo `customers` (sem duplicar código).
12. **Navegação:** entrada "Imobiliárias" adicionada ao grupo "Comercial" em `src/components/app-sidebar.tsx`; link ativo destacado ao acessar `/imobiliarias`.
13. **Responsividade:** colunas secundárias (e-mail, nome fantasia) ocultas em mobile (`< md`).
14. **Tooltips:** todos os botões de ícone possuem `<Tooltip>` com label em português (ex.: "Editar", "Excluir", "Ver detalhes").
15. **Dropdown de ações:** `DropdownMenuLabel` com "Ações" no topo; `DropdownMenuSeparator` entre grupos; itens sem ícone.
16. **Error handling:** mutations usam `throwApiError` no `mutationFn` e `handleApiError` no `onError` / `catch`.
17. **Testes unitários:** `npx vitest run src/schemas/agency.schema.test.ts src/hooks/use-agencies-table.test.ts` passa 100% — BLOQUEANTE.
18. **Testes E2E:** ~~`npx playwright test e2e/imobiliarias/`~~ — **DEFERIDO** como débito técnico `[EPIC2-T1]`. Spec criada em `tests/e2e/imobiliarias/agency-crud.spec.ts`, execução pendente. Ver `docs/STORY-BACKLOG.md`.
19. **Build e lint:** `npm run build` e `npm run lint` sem erros/warnings ao término.

## Escopo

### IN

- Páginas: lista, criação, detalhe, edição.
- Soft-delete com confirmação.
- Busca e paginação server-side.
- Navegação no sidebar.
- Testes unitários (schema + hook).
- Spec E2E criada (`tests/e2e/imobiliarias/agency-crud.spec.ts`) — execução deferida como débito técnico `[EPIC2-T1]`.

### OUT

- Relacionamento imobiliária × venda (scope do Epic 3 — §2.7).
- Relatório de comissões (bloqueado por API #95).
- Importação em lote de imobiliárias.
- Histórico de alterações da imobiliária.
- Listagem de corretores vinculados à imobiliária.

## Dependências

- **Story 2.1 (Done):** `@cacenot/construct-pro-api-client@1.0.0` instalado e paths `/api/v1/agencies` tipados.
- **Story 2.2 (InReview):** padrão canônico do módulo corretor consolidado — serve como referência direta para o módulo imobiliária.
- **Máscara CNPJ:** componente/helper já existente em `src/components/customers/` (módulo cliente PJ) — reaproveitar sem duplicar.
- **UX spec:** `docs/stories/2.3-ux-spec.md` produzido por `@ux-design-expert` antes de `@dev` iniciar.

## Estimativa

**Complexidade:** Medium (múltiplos arquivos, padrão consolidado pela Story 2.2, sem contratos API novos)
**Story Points:** 5

## Risco

**Nível:** LOW — mesmo padrão da Story 2.2 já consolidado. Sem novos contratos de API ou mudanças em features existentes.

**Mitigações:**
- Reaproveitar exatamente o padrão `clientes` e Story 2.2 como referência imediata.
- Máscara CNPJ já validada no módulo customers (PJ).
- API client 1.0.0 já tipado desde Story 2.1.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Frontend
**Secondary Type(s):** Integration (API client — `/api/v1/agencies`)
**Complexity:** Medium — 12 novos arquivos, TanStack Table, paginação server-side, formulário com validação Zod, máscaras CNPJ, campo opcional trade_name

### Specialized Agent Assignment

**Primary Agents:**
- @ux-design-expert (Phase 2.5 — design obrigatório antes de @dev)
- @dev (implementação, pre-commit reviews)
- @architect (quality gate — validação de padrão Table Pattern)

**Supporting Agents:**
- @devops (pre-PR: Playwright gate, BiomeJS lint)

### Quality Gate Tasks

- [x] UX Gate (@ux-design-expert): produzir `docs/stories/2.3-ux-spec.md` com as 5 seções obrigatórias antes de @dev iniciar
- [x] Pre-Commit (@dev): `npm run lint` + `npm run build` sem erros antes de marcar story completa
- [x] Pre-PR (@dev): `npx vitest run src/schemas/agency.schema.test.ts src/hooks/use-agencies-table.test.ts` — 100% OBRIGATÓRIO (30/30)
- [ ] Pre-PR (@devops): `npx playwright test e2e/imobiliarias/` — deferido como débito técnico `[EPIC2-T1]`
- [ ] Pre-PR (@architect): validar Table Pattern (CLAUDE.md), tooltips em português, dropdown de ações com `DropdownMenuLabel` + `DropdownMenuSeparator`

### CodeRabbit Focus Areas

**Primary Focus:**
- Acessibilidade: tooltips em português em todos os icon buttons (WCAG 2.1 AA)
- Padrão Table Pattern: `useReactTable` com `getCoreRowModel` e `manualPagination: true`
- Dropdown de ações: `DropdownMenuLabel` "Ações" + `DropdownMenuSeparator` + itens sem ícone

**Secondary Focus:**
- Responsividade: colunas ocultas com classes Tailwind (`hidden md:table-cell`)
- Error handling: `throwApiError` / `handleApiError` de `@/lib/api-error`
- Máscara CNPJ: reutilização sem duplicação (source: módulo `customers` PJ)
- Campo `trade_name` opcional: comportamento correto no form e na exibição (ausente = não renderizar)

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only (anotado em Dev Notes)
- MEDIUM/LOW: ignore

---

## Dev Notes

> **CRÍTICO:** Esta seção contém apenas informações extraídas dos artefatos de projeto existentes.
> Nenhum detalhe técnico foi inventado.

### Previous Story Insights — Story 2.2 (Corretor)

[Source: docs/stories/2.2.story.md — QA Results e Change Log]

Lições da Story 2.2 aplicáveis à 2.3:
- `PhoneInput` deve usar `international` **removido** da prop — campo já sai em E.164 nativamente.
- Schema de update deve usar `brokerUpdateSchema` / `agencyUpdateSchema` (partial), e o `mutationFn` da página de edição deve tipar `BrokerUpdateFormData` / `AgencyUpdateFormData` — **não** `AgencyCreateFormData`. (Concern levantado pelo QA em 2.2 para evitar reincidência.)
- CNPJ no PATCH: **não enviar** `cnpj` no body do `PATCH /api/v1/agencies/{id}` — campo imutável, field deve estar desabilitado no form de edição.
- Sidebar: `isValidPhoneNumber` de `libphonenumber-js` substituiu regex E.164 — garantir o mesmo na schema de agência.

### Padrão Canônico — Módulo `clientes` e Story 2.2

[Source: DIRECIONAMENTO-FRONTEND.md §1, docs/stories/2.2.story.md — Dev Notes]

Copiar exatamente a estrutura do módulo `corretores` (Story 2.2) trocando domínio:

| Camada | Caminho (corretores — referência) | Caminho (imobiliarias — a criar) |
|--------|-----------------------------------|----------------------------------|
| Página lista | `pages/corretores/+Page.tsx` | `pages/imobiliarias/+Page.tsx` |
| Página criação | `pages/corretores/novo/+Page.tsx` | `pages/imobiliarias/novo/+Page.tsx` |
| Detalhe | `pages/corretores/@id/+Page.tsx` | `pages/imobiliarias/@id/+Page.tsx` |
| Edição | `pages/corretores/@id/editar/+Page.tsx` | `pages/imobiliarias/@id/editar/+Page.tsx` |
| Colunas | `src/components/corretores/broker-columns.tsx` | `src/components/imobiliarias/agency-columns.tsx` |
| Tabela | `src/components/corretores/broker-table.tsx` | `src/components/imobiliarias/agency-table.tsx` |
| Filtros | `src/components/corretores/broker-filters.tsx` | `src/components/imobiliarias/agency-filters.tsx` |
| Paginação | `src/components/corretores/broker-pagination.tsx` | `src/components/imobiliarias/agency-pagination.tsx` |
| Formulário | `src/components/corretores/broker-form.tsx` | `src/components/imobiliarias/agency-form.tsx` |
| Hook listagem | `src/hooks/use-brokers-table.ts` | `src/hooks/use-agencies-table.ts` |
| Schema Zod | `src/schemas/broker.schema.ts` | `src/schemas/agency.schema.ts` |

### API — Agency

[Source: docs/stories/epic-2-corretores-imobiliarias.md — Campos da Imobiliária e Endpoints Agency]

**Endpoints:**

| Método | Endpoint | Uso |
|--------|----------|-----|
| `POST` | `/api/v1/agencies` | Criar imobiliária |
| `GET` | `/api/v1/agencies` | Lista paginada + busca |
| `GET` | `/api/v1/agencies/{id}` | Detalhe |
| `PATCH` | `/api/v1/agencies/{id}` | Editar |
| `DELETE` | `/api/v1/agencies/{id}` | Soft-delete |

**Campos da agency:**

| Campo | Tipo | Validação | Obrigatório |
|-------|------|-----------|-------------|
| `cnpj` | string | único, `validateCNPJ` do api-client | sim |
| `legal_name` | string | 3–160 chars | sim |
| `trade_name` | string | 0–120 chars | não |
| `creci_j` | string | 5–20 chars, único | sim |
| `email` | string | e-mail válido | não |
| `phone` | string | E.164 (stripped de máscara) | não |

**Parâmetros de busca (GET /api/v1/agencies):**
```
?search=<term>   # filtra por legal_name, trade_name, cnpj, creci_j
&page=<n>
&page_size=<n>
```

**Padrão de chamada API** (baseado em `pages/corretores/novo/+Page.tsx`):

```ts
const { client } = useApiClient()
const createMutation = useMutation({
  mutationFn: async (data) => {
    const { data: response, error } = await client.POST('/api/v1/agencies', {
      body: { ...data, cnpj: data.cnpj.replace(/\D/g, '') },
    })
    if (error) throwApiError(error, 'Falha ao cadastrar imobiliária')
    return response
  },
  onSuccess: () => { toast.success('Imobiliária cadastrada com sucesso!'); navigate('/imobiliarias') },
  onError: (error) => handleApiError(error, 'Falha ao cadastrar imobiliária'),
})
```

### Hook de Listagem — Padrão

[Source: `src/hooks/use-brokers-table.ts` (Story 2.2)]

- Usar `useQueryStates` (nuqs) com `parseAsString` / `parseAsInteger`
- `PAGE_SIZE = 10`
- Debounce de 300ms no search (via `useEffect`)
- `queryKey: ['agencies', queryParams]`
- Params de query: `{ page, page_size, search?, sort_by? }`
- Retorno: `{ data, isLoading, total, hasActiveFilters, handleClearFilters, filters, pagination }`

### Máscara CNPJ

[Source: módulo `customers` — cliente PJ (`src/schemas/customer.schema.ts`, `src/components/customers/customer-pj-form.tsx`)]

- `validateCNPJ` importado de `@cacenot/construct-pro-api-client`
- Máscara visual: `12.345.678/0001-90` — verificar `CNPJInput` em `src/components/ui/document-input.tsx`
- Ao enviar para API: strip todos os não-dígitos (`cnpj.replace(/\D/g, '')`)
- Em edição: campo CNPJ **desabilitado** (imutável — mesmo comportamento do CPF no módulo corretor)
- Schema Zod:
  ```ts
  import { validateCNPJ } from '@cacenot/construct-pro-api-client'
  const cnpjSchema = z.string().refine(
    (val) => validateCNPJ(val.replace(/\D/g, '')),
    { message: 'CNPJ inválido' }
  )
  ```

### Telefone

[Source: `src/components/corretores/broker-form.tsx` (Story 2.2 — fix FIX-2.2-002)]

- **Componente:** `<PhoneInput>` de `src/components/ui/phone-input.tsx`
- **Prop `international` removida** (fix da Story 2.2 — evitar reincidência)
- `defaultCountry="BR"` mantido
- Valor já sai em E.164 pelo componente
- Schema: usar `isValidPhoneNumber` de `libphonenumber-js` (não regex manual)

### Navegação — Sidebar

[Source: `src/components/app-sidebar.tsx` — estado atual após Story 2.2]

`navComercial` array (linha ~37 após Story 2.2):
```ts
const navComercial = [
  { title: 'Vendas', href: '/vendas', icon: ShoppingCart },
  { title: 'Contratos', href: '/contratos', icon: FileText },
  { title: 'Corretores', href: '/corretores', icon: UserCheck },
  // ADICIONAR:
  { title: 'Imobiliárias', href: '/imobiliarias', icon: Building2 }, // Building2 já importado em navOperacoes
]
```

> **Atenção:** `Building2` já está importado no arquivo (usado em `navOperacoes` para Empreendimentos). Usar ícone diferente se o UX spec recomendar outro — verificar com `@ux-design-expert`.

### Table Pattern

[Source: CLAUDE.md — Table Pattern]

- `useReactTable` com `getCoreRowModel()` e `manualPagination: true`
- Skeleton rows durante loading
- Colunas responsivas: `hidden md:table-cell` no cell renderer
- Dropdown de ações: `DropdownMenuLabel` "Ações" + `DropdownMenuSeparator` + itens sem ícone
- Wrap do trigger do dropdown em `<Tooltip>`
- Coluna `trade_name`: exibir `—` (em dash) quando ausente/null

### Schema Zod — agency.schema.ts

[Source: docs/stories/epic-2-corretores-imobiliarias.md, broker.schema.ts (Story 2.2)]

```ts
export const agencyCreateSchema = z.object({
  cnpj: z.string().refine((val) => validateCNPJ(val.replace(/\D/g, '')), { message: 'CNPJ inválido' }),
  legal_name: z.string().min(3, 'Mínimo 3 caracteres').max(160, 'Máximo 160 caracteres'),
  trade_name: z.string().max(120, 'Máximo 120 caracteres').optional().or(z.literal('')),
  creci_j: z.string().min(5, 'Mínimo 5 caracteres').max(20, 'Máximo 20 caracteres'),
  email: z.string().email('E-mail inválido').optional().or(z.literal('')),
  phone: z.string().refine(
    (val) => !val || isValidPhoneNumber(val),
    { message: 'Telefone inválido' }
  ).optional().or(z.literal('')),
})

export const agencyUpdateSchema = agencyCreateSchema.omit({ cnpj: true }).partial()

export type AgencyCreateFormData = z.infer<typeof agencyCreateSchema>
export type AgencyUpdateFormData = z.infer<typeof agencyUpdateSchema>
```

### Testes Unitários — Cobertura Mínima

[Source: docs/stories/epic-2-corretores-imobiliarias.md — Testes Unitários §2.3]

`src/schemas/agency.schema.test.ts`:
- CNPJ inválido rejeitado / CNPJ válido aceito
- `legal_name` com 2 chars rejeitado, 3 aceito, 160 aceito, 161 rejeitado
- `trade_name` com 121 chars rejeitado, 120 aceito, ausente aceito (opcional)
- `creci_j` com 4 chars rejeitado, 5 aceito, 20 aceito, 21 rejeitado
- `email` inválido rejeitado, válido aceito, ausente aceito (opcional)
- `phone` inválido rejeitado, E.164 válido aceito, ausente aceito (opcional)

`src/hooks/use-agencies-table.test.ts`:
- Hook retorna `{ data, isLoading, total, hasActiveFilters, ... }`
- `handleClearFilters` reseta search e page

### Testes E2E — Cenários Planejados (Deferidos)

[Source: docs/stories/epic-2-corretores-imobiliarias.md — Testes E2E §2.3]

Localização: `tests/e2e/imobiliarias/`

Cenários mínimos:
1. Lista paginada carrega sem erro
2. Busca por razão social filtra resultados
3. Busca por CNPJ filtra resultados
4. Criação completa (fill form → submit → toast sucesso → redirect lista)
5. Edição (navegar detalhe → editar → submit → toast sucesso)
6. Soft-delete com confirmação (botão excluir → AlertDialog → confirmar → toast → redirect lista)

### Arquivos Existentes a Modificar

- `src/components/app-sidebar.tsx` — adicionar `{ title: 'Imobiliárias', href: '/imobiliarias', icon: <ícone> }` em `navComercial`

### Informação Não Encontrada / Pendente de @ux-design-expert

- Ícone específico para "Imobiliária" no sidebar: `Building2` disponível (já importado), mas pode conflitar visualmente com "Empreendimentos". @ux-design-expert deve recomendar.
- Layout da página de detalhe: tabs ou flat (como corretor). Imobiliária não tem sub-entidades no escopo desta story → provável flat, mas confirmar com @ux-design-expert.
- Placeholder do campo `trade_name`: decidir entre `"Nome fantasia (opcional)"` ou deixar vazio.

---

## Tasks / Subtasks

### Task 1 — Verificar UX spec (AC: todos)

- [x] Confirmar existência de `docs/stories/2.3-ux-spec.md` com as 5 seções obrigatórias
- [x] Se ausente: invocar `@ux-design-expert *design-story 2.3` e AGUARDAR antes de prosseguir

### Task 2 — Schema Zod `agency.schema.ts` (AC: 10)

- [x] Criar `src/schemas/agency.schema.ts`
- [x] Definir `agencyCreateSchema` com: `cnpj` (validateCNPJ), `legal_name` (3–160), `trade_name` (opcional, 0–120), `creci_j` (5–20), `email` (opcional), `phone` (opcional E.164 via isValidPhoneNumber)
- [x] Definir `agencyUpdateSchema` (omit `cnpj`, campos opcionais — partial)
- [x] Exportar tipos: `AgencyCreateFormData`, `AgencyUpdateFormData`
- [x] Reutilizar `validateCNPJ` de `@cacenot/construct-pro-api-client`
- [x] Usar `isValidPhoneNumber` de `libphonenumber-js` (não regex manual — lesson de Story 2.2)

### Task 3 — Hook `use-agencies-table.ts` (AC: 2, 3, 4, 5)

- [x] Criar `src/hooks/use-agencies-table.ts` copiando padrão de `use-brokers-table.ts`
- [x] `queryKey: ['agencies', queryParams]`
- [x] Params: `page`, `page_size`, `search` (debounce 300ms) — busca cobre `legal_name`, `trade_name`, `cnpj`, `creci_j`
- [x] Endpoint: `GET /api/v1/agencies`
- [x] Retornar interface padrão: `{ data, isLoading, total, hasActiveFilters, handleClearFilters, filters, pagination }`

### Task 4 — Componentes de tabela (AC: 1, 4, 5, 13, 14, 15)

- [x] Criar `src/components/imobiliarias/agency-columns.tsx`
  - Colunas: razão social, nome fantasia (`—` se ausente), CNPJ (mascarado), CRECI-J, e-mail, ações
  - `trade_name` e `email`: `hidden md:table-cell`
- [x] Criar `src/components/imobiliarias/agency-table.tsx`
- [x] Criar `src/components/imobiliarias/agency-filters.tsx`
- [x] Criar `src/components/imobiliarias/agency-pagination.tsx`

### Task 5 — Formulário `agency-form.tsx` (AC: 6, 8, 10, 11)

- [x] Criar `src/components/imobiliarias/agency-form.tsx`
- [x] Campos: CNPJ (mascarado, desabilitado em edição), razão social, nome fantasia (opcional), CRECI-J, e-mail (opcional), telefone (opcional)
- [x] Reutilizar `CNPJInput` de `src/components/ui/document-input.tsx`
- [x] Reutilizar `PhoneInput` de `src/components/ui/phone-input.tsx` (sem prop `international`)
- [x] Prop `isEdit?: boolean` para desabilitar campo CNPJ na edição

### Task 6 — Páginas Vike (AC: 1, 6, 7, 8, 9, 12)

- [x] Criar `pages/imobiliarias/+Page.tsx`
- [x] Criar `pages/imobiliarias/novo/+Page.tsx`
- [x] Criar `pages/imobiliarias/@id/+Page.tsx`
- [x] Criar `pages/imobiliarias/@id/editar/+Page.tsx`

### Task 7 — Navegação (AC: 12)

- [x] Adicionar `{ title: 'Imobiliárias', href: '/imobiliarias', icon: Landmark }` ao `navComercial` em `src/components/app-sidebar.tsx`

### Task 8 — Testes unitários (AC: 17)

- [x] Criar `src/schemas/agency.schema.test.ts` — cobrir todos os casos de validação
- [x] Criar `src/hooks/use-agencies-table.test.ts` — retorno do hook e clearFilters
- [x] Executar: `npx vitest run src/schemas/agency.schema.test.ts src/hooks/use-agencies-table.test.ts` → 100% (30 testes)

### Task 9 — Testes E2E (AC: 18) — DEFERIDO `[EPIC2-T1]`

- [x] Criar `tests/e2e/imobiliarias/agency-crud.spec.ts` com 6+ cenários (lista, busca por razão social, busca por CNPJ, criação, edição, soft-delete)
- [ ] Executar: `npx playwright test tests/e2e/imobiliarias/` → **DEFERIDO** como débito técnico (ver `docs/STORY-BACKLOG.md`)

### Task 10 — Quality Gates finais (AC: 19)

- [x] `npm run lint` → zero warnings
- [x] `npm run build` → zero erros TypeScript
- [x] Revisar: todos os icon buttons têm `<Tooltip>` em português
- [x] Dropdown de ações segue padrão (Label "Ações" + Separator + sem ícones nos itens)
- [x] CNPJ não enviado no body do PATCH (campo omitido no `agencyUpdateSchema`)
- [ ] CodeRabbit pre-commit (requer CLI instalado via wsl)

---

## File List

### Novos arquivos a criar

```
pages/imobiliarias/+Page.tsx
pages/imobiliarias/novo/+Page.tsx
pages/imobiliarias/@id/+Page.tsx
pages/imobiliarias/@id/editar/+Page.tsx
src/components/imobiliarias/agency-columns.tsx
src/components/imobiliarias/agency-table.tsx
src/components/imobiliarias/agency-filters.tsx
src/components/imobiliarias/agency-pagination.tsx
src/components/imobiliarias/agency-form.tsx
src/hooks/use-agencies-table.ts
src/schemas/agency.schema.ts
src/schemas/agency.schema.test.ts
src/hooks/use-agencies-table.test.ts
tests/e2e/imobiliarias/agency-crud.spec.ts   (spec criada, execução deferida [EPIC2-T1])
docs/stories/2.3-ux-spec.md                  (produzido por @ux-design-expert)
```

### Arquivos existentes a modificar

```
src/components/app-sidebar.tsx        (adicionar "Imobiliárias" ao navComercial)
```

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-05-30 | @sm (River) | Story criada em Draft a partir do Epic 2 + padrão da Story 2.2 |
| 2026-05-30 | @po (Pax) | Validação GO (10/10) — Status: Draft → Ready |
| 2026-05-30 | @ux-design-expert (Uma) | UX spec produzido: `docs/stories/2.3-ux-spec.md` — UX Gate ✅ desbloqueado para @dev |
| 2026-05-30 | @dev (Dex) | Development started (yolo mode) — Status: Ready → InProgress |
| 2026-05-30 | @dev (Dex) | Development complete — 16 arquivos criados, 30 testes passando, lint+build OK — Status: InProgress → InReview |
